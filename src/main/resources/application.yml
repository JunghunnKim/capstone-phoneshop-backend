jwt:
  secret: Y2Fwc3RvbmUtMjAyNi1waG9uZS10ZWFtLXNlY3JldC1qd3Qta2V5
  expiration: 3600000

kakaopay:
  secretKey: ${KAKAOPAY_SECRET_KEY}
  cid: TC0ONETIME

app:
  base-url: ${BASE_URL}

spring:

  datasource:
    url: jdbc:mysql://localhost:3306/phoneshop?serverTimezone=Asia/Seoul
    username: root
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
  servlet:
    context-path: /api