jwt:
  secret: capstone-2026-phone-team-secret-jwt-key
  expiration: 3600000

spring:

  datasource:
    url: jdbc:mysql://localhost:3306/phoneshop?serverTimezone=Asia/Seoul
    username: root
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
